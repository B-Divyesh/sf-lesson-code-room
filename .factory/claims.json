[
  {
    "id": "anonymous-room",
    "claim": "Learners join without accounts, and teachers see Joined, Ran code, or Done.",
    "where": "landing first screen and how-it-works section",
    "test": "npm test -- --grep @claim:anonymous-room",
    "sandbox": "fresh browser context opened at /demo; join through its learner link and run the page"
  },
  {
    "id": "custom-room",
    "claim": "Teachers can create a room with custom HTML, CSS, and JavaScript that each learner receives as an editable copy.",
    "where": "landing creator",
    "test": "npm test -- --grep @claim:custom-room",
    "sandbox": "fresh landing page; replace the starter HTML, create a room, join from a second page, and assert the editable copy"
  },
  {
    "id": "sandbox-run",
    "claim": "Editable HTML, CSS, and JavaScript run in a network-blocked browser sandbox.",
    "where": "creator and learner workbench",
    "test": "npm test -- --grep @claim:sandbox-run",
    "sandbox": "fresh /demo learner; change HTML and JavaScript, run it, and assert an attempted external request never leaves"
  },
  {
    "id": "demo-reset",
    "claim": "The demo uses temporary sample data and resets to a fresh room.",
    "where": "/demo banner",
    "test": "npm test -- --grep @claim:demo-reset",
    "sandbox": "fresh /demo; reset and compare room links while checking seeded learners"
  },
  {
    "id": "learner-reset",
    "claim": "A learner can reset all three edited files and the preview to the teacher's starter code.",
    "where": "README and learner workbench",
    "test": "npm test -- --grep @claim:learner-reset",
    "sandbox": "fresh /demo learner; edit HTML, CSS, and JavaScript, run the changed page, accept reset, then assert all starter fields and the starter preview are restored"
  },
  {
    "id": "privacy-code",
    "claim": "Learner edits stay in the learner's browser and are absent from progress updates.",
    "where": "/privacy",
    "test": "npm test -- --grep @claim:privacy-code",
    "sandbox": "fresh /demo learner; insert a unique code marker and assert it is absent from the progress request"
  },
  {
    "id": "teacher-report-limits",
    "claim": "Teacher reports contain screen names and Joined, Ran code, or Done states, with no grades or detailed activity.",
    "where": "landing surveillance section",
    "test": "npm test -- --grep @claim:teacher-report-limits",
    "sandbox": "fresh demo room; join and run, then inspect the teacher progress response fields and allowed states"
  },
  {
    "id": "product-scope",
    "claim": "The workbench runs HTML, CSS, and JavaScript, with no repository import or video-call controls and camera and microphone blocked.",
    "where": "landing limits, learner workbench, and /privacy",
    "test": "npm test -- --grep @claim:product-scope",
    "sandbox": "fresh /demo learner; inspect available editor tabs, controls, media elements, file inputs, and response permissions policy"
  },
  {
    "id": "no-tracking",
    "claim": "Lesson Code Room pages load no advertising trackers.",
    "where": "/privacy",
    "test": "npm test -- --grep @claim:no-tracking",
    "sandbox": "fresh context; record every request through landing, privacy, demo, join, and run, and assert every origin is the product origin"
  },
  {
    "id": "session-storage",
    "claim": "A learner room key stays in the current browser tab and is not persistent browser storage.",
    "where": "/privacy",
    "test": "npm test -- --grep @claim:session-storage",
    "sandbox": "fresh /demo learner; join, inspect session and local storage, then open the room in another tab"
  },
  {
    "id": "offline-preview",
    "claim": "After the workbench loads, editing and preview keep working while offline.",
    "where": "learner workbench offline state",
    "test": "npm test -- --grep @claim:offline-preview",
    "sandbox": "fresh /demo learner; warm the preview, take the browser offline, edit HTML, run, and assert the changed preview"
  },
  {
    "id": "rate-limit",
    "claim": "API request bursts are limited and each 429 response says to retry after one second.",
    "where": "/privacy and README",
    "test": "npm test -- --grep @claim:rate-limit",
    "sandbox": "fresh client identity; send 48 simultaneous demo requests and assert limited responses include Retry-After: 1"
  },
  {
    "id": "free-capacity",
    "claim": "Free rooms allow 10 learners.",
    "where": "landing facts and paid section",
    "test": "npm test -- --grep @claim:free-capacity",
    "sandbox": "fresh /demo API response; assert room capacity"
  },
  {
    "id": "room-retention",
    "claim": "Live rooms expire after 24 hours.",
    "where": "landing facts, creator, and /privacy",
    "test": "npm test -- --grep @claim:room-retention",
    "sandbox": "fresh API room using sample code; assert its expiry timestamp is 24 hours ahead"
  },
  {
    "id": "demo-retention",
    "claim": "Demo rooms expire after two hours.",
    "where": "/privacy",
    "test": "npm test -- --grep @claim:demo-retention",
    "sandbox": "fresh /demo API response; assert its expiry timestamp is two hours ahead"
  },
  {
    "id": "paid-checkout",
    "claim": "Room Plus is a $29 one-time license that raises new rooms to 30 learners and uses Sociobot's hosted checkout.",
    "where": "landing paid section, /privacy, and /terms",
    "test": "npm test -- --grep @claim:paid-checkout",
    "sandbox": "fresh landing page; assert the Sociobot checkout target resolves to its hosted checkout, restore against a recorded valid response, and inspect stored token and verdict"
  }
]
